---
phase: 01-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ppt_skill/converter/__init__.py
  - src/ppt_skill/converter/cli.py
  - src/ppt_skill/converter/builder.py
  - src/ppt_skill/converter/converter.py
  - src/ppt_skill/converter/context.py
  - src/ppt_skill/converter/elements.py
  - src/ppt_skill/converter/paths.py
  - src/ppt_skill/converter/styles.py
  - src/ppt_skill/converter/utils.py
  - src/ppt_skill/converter/dimensions.py
  - src/ppt_skill/converter/discovery.py
  - src/ppt_skill/converter/media.py
  - src/ppt_skill/converter/narration.py
  - src/ppt_skill/converter/notes.py
  - src/ppt_skill/converter/slide_xml.py
  - src/ppt_skill/converter/use_expander.py
  - src/ppt_skill/converter/tspan_flattener.py
  - src/ppt_skill/finalize/__init__.py
  - src/ppt_skill/finalize/embed_icons.py
  - src/ppt_skill/finalize/align_embed_images.py
  - src/ppt_skill/finalize/crop_images.py
  - src/ppt_skill/finalize/embed_images.py
  - src/ppt_skill/finalize/fix_image_aspect.py
  - src/ppt_skill/finalize/flatten_tspan.py
  - src/ppt_skill/finalize/svg_rect_to_path.py
autonomous: true
requirements:
  - PIP-01
  - PIP-03

must_haves:
  truths:
    - "All 17 converter modules import without errors (no ImportError for renamed cross-references)"
    - "All 8 finalize modules import without errors"
    - "use_expander.py can import svg_finalize.embed_icons for icon expansion"
    - "tspan_flattener.py can import svg_finalize.flatten_tspan for text processing"
    - "Converter resolves template paths relative to the new package structure, not ppt-master's 3-parent traversal"
  artifacts:
    - path: "src/ppt_skill/converter/__init__.py"
      provides: "Converter package marker"
      min_lines: 1
    - path: "src/ppt_skill/converter/converter.py"
      provides: "DrawingML conversion dispatcher (renamed from drawingml_converter.py)"
      contains: "convert_svg_to_slide_shapes"
    - path: "src/ppt_skill/converter/builder.py"
      provides: "PPTX assembly with native shapes (renamed from pptx_builder.py)"
      contains: "create_pptx_with_native_svg"
    - path: "src/ppt_skill/converter/elements.py"
      provides: "DrawingML element converters (rectangle, circle, path, text, image)"
      contains: "convert_element"
    - path: "src/ppt_skill/finalize/embed_icons.py"
      provides: "In-memory icon expansion from templates"
      contains: "resolve_data_icon"
  key_links:
    - from: "src/ppt_skill/converter/converter.py"
      to: "src/ppt_skill/finalize/embed_icons.py"
      via: "use_expander.py import chain"
      pattern: "from.*finalize.*import.*embed_icons"
    - from: "src/ppt_skill/converter/tspan_flattener.py"
      to: "src/ppt_skill/finalize/flatten_tspan.py"
      via: "direct import for in-memory text normalization"
      pattern: "from.*finalize.*import.*flatten_tspan"
    - from: "src/ppt_skill/converter/converter.py"
      to: "templates/icons/"
      via: "use_expander.py resolve_data_icon()"
      pattern: "templates.*icons"
---

<objective>
Fork ppt-master's 17-module svg_to_pptx/ converter package and 8-module svg_finalize/ post-processing package into the ppt-skill project. Fix all imports, module references, and hardcoded path resolution to work within the new package structure. The cross-package dependency between converter/use_expander.py and finalize/embed_icons.py (and converter/tspan_flattener.py ← finalize/flatten_tspan.py) must remain intact — breaking these dual-consumer links silently corrupts native PPTX output.

Purpose: Establish the forked execution engine as a standalone Python package ready for integration testing.
Output: Two working packages (converter/ and finalize/) under `src/ppt_skill/` with all modules importing successfully.
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork svg_to_pptx/ converter modules (17 files) into src/ppt_skill/converter/</name>
  <files>
    src/ppt_skill/converter/__init__.py
    src/ppt_skill/converter/cli.py
    src/ppt_skill/converter/builder.py
    src/ppt_skill/converter/converter.py
    src/ppt_skill/converter/context.py
    src/ppt_skill/converter/elements.py
    src/ppt_skill/converter/paths.py
    src/ppt_skill/converter/styles.py
    src/ppt_skill/converter/utils.py
    src/ppt_skill/converter/dimensions.py
    src/ppt_skill/converter/discovery.py
    src/ppt_skill/converter/media.py
    src/ppt_skill/converter/narration.py
    src/ppt_skill/converter/notes.py
    src/ppt_skill/converter/slide_xml.py
    src/ppt_skill/converter/use_expander.py
    src/ppt_skill/converter/tspan_flattener.py
  </files>
  <action>
    **Source:** `/Users/wuliang/project/ppt-master/skills/ppt-master/scripts/svg_to_pptx/`

    Create directory `src/ppt_skill/converter/`, then copy each module with the following rename mapping:

    | Original (ppt-master) | New (ppt-skill) |
    |---|---|
    | `__init__.py` | `__init__.py` (add package marker comment) |
    | `pptx_cli.py` | `cli.py` |
    | `pptx_builder.py` | `builder.py` |
    | `drawingml_converter.py` | `converter.py` |
    | `drawingml_context.py` | `context.py` |
    | `drawingml_elements.py` | `elements.py` |
    | `drawingml_paths.py` | `paths.py` |
    | `drawingml_styles.py` | `styles.py` |
    | `drawingml_utils.py` | `utils.py` |
    | `pptx_dimensions.py` | `dimensions.py` |
    | `pptx_discovery.py` | `discovery.py` |
    | `pptx_media.py` | `media.py` |
    | `pptx_narration.py` | `narration.py` |
    | `pptx_notes.py` | `notes.py` |
    | `pptx_slide_xml.py` | `slide_xml.py` |
    | `use_expander.py` | `use_expander.py` (keep name) |
    | `tspan_flattener.py` | `tspan_flattener.py` (keep name) |

    **During copy:** Do NOT modify code yet — pure file copy with renaming. All import/refactoring happens in Task 3. This preserves a clean diff boundary between "what was copied" and "what was changed."

    Use `cp` to copy each file individually (not `shutil.copytree`) to ensure exact control over destination names.
  </action>
  <verify>All 17 files exist in `src/ppt_skill/converter/` with correct names. `ls src/ppt_skill/converter/*.py | wc -l` returns 17.</verify>
  <done>17 renamed files from svg_to_pptx/ exist in src/ppt_skill/converter/ — content identical to source, no modifications yet.</done>
</task>

<task type="auto">
  <name>Task 2: Fork svg_finalize/ post-processing modules (8 files) into src/ppt_skill/finalize/</name>
  <files>
    src/ppt_skill/finalize/__init__.py
    src/ppt_skill/finalize/embed_icons.py
    src/ppt_skill/finalize/align_embed_images.py
    src/ppt_skill/finalize/crop_images.py
    src/ppt_skill/finalize/embed_images.py
    src/ppt_skill/finalize/fix_image_aspect.py
    src/ppt_skill/finalize/flatten_tspan.py
    src/ppt_skill/finalize/svg_rect_to_path.py
  </files>
  <action>
    **Source:** `/Users/wuliang/project/ppt-master/skills/ppt-master/scripts/svg_finalize/`

    Create directory `src/ppt_skill/finalize/`, then copy all 8 modules with original names (no rename needed — these module names are already clean).

    **During copy:** Pure file copy, no modifications. Do NOT copy `finalize_svg.py` from the parent `scripts/` directory — that's the disk-pipeline orchestrator, not a core module. The in-memory pipeline (converter/ → finalize/) does not use it.

    Note: These 8 modules form the in-memory post-processing pipeline that the converter calls via `use_expander.py` and `tspan_flattener.py` during native-shape conversion. They must be a sibling package to converter/.
  </action>
  <verify>All 8 files exist in `src/ppt_skill/finalize/`. `ls src/ppt_skill/finalize/*.py | wc -l` returns 8.</verify>
  <done>8 finalize modules from svg_finalize/ exist in src/ppt_skill/finalize/ — content identical to source, no modifications yet.</done>
</task>

<task type="auto">
  <name>Task 3: Fix all imports, path resolution, and cross-package references</name>
  <files>
    src/ppt_skill/converter/converter.py
    src/ppt_skill/converter/builder.py
    src/ppt_skill/converter/context.py
    src/ppt_skill/converter/elements.py
    src/ppt_skill/converter/paths.py
    src/ppt_skill/converter/styles.py
    src/ppt_skill/converter/utils.py
    src/ppt_skill/converter/dimensions.py
    src/ppt_skill/converter/discovery.py
    src/ppt_skill/converter/media.py
    src/ppt_skill/converter/narration.py
    src/ppt_skill/converter/notes.py
    src/ppt_skill/converter/slide_xml.py
    src/ppt_skill/converter/use_expander.py
    src/ppt_skill/converter/tspan_flattener.py
    src/ppt_skill/finalize/embed_icons.py
  </files>
  <action>
    Apply targeted fixes to make all modules importable under the new `ppt_skill.converter` and `ppt_skill.finalize` package names. **Do NOT modify conversion logic — only fix imports, paths, and module references.**

    **Fix 1 — Internal converter imports (all .py files in converter/):**
    Replace all old relative imports with new package-relative imports:
    - `from drawingml_converter import ...` → `from ppt_skill.converter.converter import ...`
    - `from drawingml_context import ...` → `from ppt_skill.converter.context import ...`
    - `from drawingml_elements import ...` → `from ppt_skill.converter.elements import ...`
    - `from drawingml_paths import ...` → `from ppt_skill.converter.paths import ...`
    - `from drawingml_styles import ...` → `from ppt_skill.converter.styles import ...`
    - `from drawingml_utils import ...` → `from ppt_skill.converter.utils import ...`
    - `from pptx_builder import ...` → `from ppt_skill.converter.builder import ...`
    - `from pptx_cli import ...` → `from ppt_skill.converter.cli import ...`
    - `from pptx_dimensions import ...` → `from ppt_skill.converter.dimensions import ...`
    - `from pptx_discovery import ...` → `from ppt_skill.converter.discovery import ...`
    - `from pptx_media import ...` → `from ppt_skill.converter.media import ...`
    - `from pptx_notes import ...` → `from ppt_skill.converter.notes import ...`
    - `from pptx_narration import ...` → `from ppt_skill.converter.narration import ...`
    - `from pptx_slide_xml import ...` → `from ppt_skill.converter.slide_xml import ...`
    - `from use_expander import ...` → `from ppt_skill.converter.use_expander import ...`
    - `from tspan_flattener import ...` → `from ppt_skill.converter.tspan_flattener import ...`

    **Fix 2 — Cross-package imports (use_expander.py and tspan_flattener.py):**
    These two modules import from svg_finalize/ in-memory. Fix to use new package path:
    - In `src/ppt_skill/converter/use_expander.py`: Replace `from svg_finalize.embed_icons import ...` with `from ppt_skill.finalize.embed_icons import ...`
    - In `src/ppt_skill/converter/tspan_flattener.py`: Replace `from svg_finalize.flatten_tspan import ...` with `from ppt_skill.finalize.flatten_tspan import ...`

    **Fix 3 — Template path resolution (5 specific locations from RESEARCH.md §Pitfall 2):**
    Each module resolves `templates/icons/` using `Path(__file__).resolve().parent.parent.parent`. After forking, this 3-parent traversal points wrong. Fix each:
    - `converter/converter.py` (~line 308 `icons_dir`): Change to resolve from a package-level `TEMPLATES_DIR` or use `Path(__file__).resolve().parent.parent.parent / 'templates' / 'icons'` after verifying it points to the project root
    - `converter/use_expander.py` (~line 33 `scripts_dir`): This was used to import svg_finalize during path resolution. Since we're using package imports now, remove the path manipulation for svg_finalize import. Keep the templates path resolution.
    - `converter/tspan_flattener.py` (~line 39 `scripts_dir`): Same as above — remove svg_finalize path hack, keep templates path.
    - `finalize/embed_icons.py` (~line 52 `DEFAULT_ICONS_DIR`): Update to resolve correctly from the finalize/ package.
    - Document the correct template path resolution in a comment at the top of converter/__init__.py: `# Template resolution: templates/ lives at project root (sibling to src/). Use: Path(__file__).resolve().parent.parent.parent / 'templates' / 'icons'`

    **Fix 4 — config.py imports (dimensions.py, discovery.py, media.py):**
    These modules may have soft imports of `config.py` (try/except ImportError). Remove these try/except blocks — Task 5 in Plan 02 will create a proper `ppt_skill.config` module. For now, strip the imports so they don't fail on missing ppt-master config. The only essential config (`CANVAS_FORMATS`) will be provided by Plan 02.
    - Search for `from config import` or `import config` in all converter/ files
    - Remove try/except ImportError blocks that reference config attributes not essential to conversion
    - Keep try/except blocks that handle soft deps like cairosvg or pptx_animations (those are valid optional renderers)

    **Fix 5 — Strip ppt-master-specific modules from imports:**
    - `converter/cli.py`: May import project_manager.py, image_gen.py, notes_to_audio.py, or other ppt-master-specific modules. Comment out or remove these imports — Phase 1 only needs the converter, not the full project manager.
    - `converter/slide_xml.py`: This is the legacy compatibility mode (flattened SVG→PNG). Keep the file but the imports may reference ppt-master paths. Fix any broken relative imports.
  </action>
  <verify>
    Run the following verification from the project root:
    ```bash
    cd /Users/wuliang/workspace/ppt-skill && python -c "
    from ppt_skill.converter.converter import convert_svg_to_slide_shapes
    from ppt_skill.converter.builder import create_pptx_with_native_svg
    from ppt_skill.converter.elements import ConvertContext
    from ppt_skill.converter.utils import parse_hex_color
    from ppt_skill.finalize.embed_icons import resolve_data_icon
    from ppt_skill.finalize.flatten_tspan import flatten_tspans
    print('ALL IMPORTS OK')
    "
    ```
    All six imports must succeed without ImportError or ModuleNotFoundError.
  </verify>
  <done>All 25 modules import successfully. Cross-package links (use_expander → finalize, tspan_flattener → finalize) resolve. No hardcoded ppt-master paths remain.</done>
</task>

</tasks>

<verification>
**Phase-level integration check (after Plan 01-03 completion):**
1. All 17 converter/ modules import under `ppt_skill.converter.*`
2. All 8 finalize/ modules import under `ppt_skill.finalize.*`
3. `use_expander.py` can resolve `resolve_data_icon` from `ppt_skill.finalize.embed_icons`
4. `tspan_flattener.py` can call `flatten_tspans` from `ppt_skill.finalize.flatten_tspan`
5. No `from scripts.svg_to_pptx` or `from scripts.svg_finalize` references remain anywhere
</verification>

<success_criteria>
- `python -c "from ppt_skill.converter.converter import convert_svg_to_slide_shapes"` succeeds
- `python -c "from ppt_skill.finalize.embed_icons import resolve_data_icon"` succeeds
- Grep for `drawingml_` in `src/ppt_skill/converter/*.py` returns zero matches (all old module names replaced)
- Grep for `pptx_` import statements (not filenames) in converter/ shows only `ppt_skill.converter.*` patterns
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-foundation/01-01-SUMMARY.md`
</output>

---

*Phase 1, Plan 01 — no user checkpoints required. Fully autonomous. Proceed to Plan 02 after completion.*
