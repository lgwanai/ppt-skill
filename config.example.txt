# PPT Skill Configuration
# Copy to config.txt and fill in your settings.
# The skill reads config.txt from the project root directory.

# ── Vision-Language Model (for spec extraction) ─────────────────────

# Provider: openai | anthropic | gemini | ollama
VL_PROVIDER=openai

# Model name
VL_MODEL=gpt-4o

# API key (required for openai/anthropic/gemini)
VL_API_KEY=sk-your-key-here

# Custom API base (for ollama: http://localhost:11434/v1)
VL_API_BASE=

# Maximum tokens for VL analysis response
VL_MAX_TOKENS=4096

# Set to "true" to enable VL analysis during spec extraction
VL_ENABLED=true

# ── Extraction Settings ─────────────────────────────────────────────

# Output resolution for page screenshots (PX width)
SCREENSHOT_WIDTH=1920

# DPI for PPTX-to-image conversion
SCREENSHOT_DPI=150

# Skip pages that are 90%+ similar to previously processed pages
DEDUP_SIMILARITY_THRESHOLD=0.9
